const moment = require('moment/moment');
const {

} = require('./index');


const ValidInputForProjectCreate = (userObjParam) => {
};
const CommentsValidation = (comment) => {
};
const likesValidation = (userObjParam) => {
};
module.exports = {
	ValidInputForProjectCreate,
    CommentsValidation,
    likesValidation,
};
